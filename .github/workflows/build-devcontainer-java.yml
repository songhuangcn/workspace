name: build-devcontainer-java

on:
  push:
    branches:
      - main
    paths:
      - .devcontainer/Dockerfile.java
      - .github/workflows/build-devcontainer-java.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  docker:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Get short commit SHA
        id: vars
        run: echo "short_sha=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .devcontainer
          file: .devcontainer/Dockerfile.java
          platforms: linux/arm64
          push: true
          tags: |
            songhuangcn/workspace-java:${{ steps.vars.outputs.short_sha }}
            songhuangcn/workspace-java:latest
