{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "*": {
      "*": "allow"
    }
  },
  "disabled_providers": ["opencode"],
  // 用来给会话自动取名的模型，不设置的话会自己猜测 nano 哪些小模型，如果代理站没有，就会设置不成功
  "small_model": "openai/gpt-5.5"
}